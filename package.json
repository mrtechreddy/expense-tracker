{
  "name": "expense-trackr",
  "version": "1.0.0",
  "description": "ExpenseTrackr - Node.js expense tracker (Express + SQLite + EJS)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r server.js package.json package-lock.json public views routes services config db dist && cd dist && npm install --omit=dev && echo '✅ Build complete: dist with production dependencies created.'"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.2",
    "connect-sqlite3": "^0.9.16",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.9",
    "express": "^4.22.0",
    "express-ejs-layouts": "^2.5.1",
    "express-session": "^1.17.3",
    "moment": "^2.29.4",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "author": "Your Name",
  "license": "MIT"
}

